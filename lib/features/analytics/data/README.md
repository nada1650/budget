# Analytics Data Layer
This folder contains models, remote/local data sources, and repository implementations specific to the Analytics feature if it connects to external APIs. Otherwise, it uses the shared Transactions feature logic.
